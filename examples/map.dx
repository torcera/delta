struct MapEntry {
    key: int;
    value: int;
    occupied: bool;
}

struct Map {
    entries: MapEntry[];
    size: int;
}

fn map_get(m: Map, key: int) -> int {
    var result = m.entries[key].value;
    return result;
}

fn main() -> void {
    var entry = MapEntry { key: 1, value: 52, occupied: false };
    var my_map = Map { entries: [entry], size: 1 };
    var result = map_get(my_map, 0);
    var x = result + 1;
}
